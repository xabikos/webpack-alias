import React, { Component } from 'react';

export default class Utility extends Component {
  render() {
    return (
      <div>This is a utility component that is used by multiple components.</div>
    );
  }
}
